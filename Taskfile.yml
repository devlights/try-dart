# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      - dart --version
    silent: false
  init:
    cmds:
      - dart create . --force
      - git restore README.md
  clean:
    cmds:
      - rm -rf bin/ lib/ test/ .dart_tool/
      - rm -f analysis_options.yaml pubspec.*
  run:
    cmds:
      - dart run

